class Product{
  final String image, title, description, id;
  final int price, size, color;
  Product({
    this.id,
    this.image,
    this.title,
    this.price,
    this.description,
    this.size,
    this.color,
  });
}
List<Product> upper = List<Product>();
List<Product> lower = List<Product>();
List<Product> saree = List<Product>();
List<Product> dress = List<Product>();
